{
  "name": "cyberxtrmx",
  "version": "5.3.5",
  "private": true,
  "description": "CYBERTRMX mobile-first security operations workspace.",
  "scripts": {
    "check": "node --check public/app.js",
    "test": "node --test tests/*.test.cjs",
    "test:ui": "playwright test"
  },
  "devDependencies": {
    "@playwright/test": "1.54.2"
  },
  "engines": {
    "node": ">=20"
  }
}