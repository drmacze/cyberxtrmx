{
  "name": "cyberxtrmx",
  "version": "1.0.0",
  "private": true,
  "description": "CYBERTRMX isolated cyber operations simulator with safe public-data adapters.",
  "type": "module",
  "scripts": {
    "start": "node server.js",
    "dev": "node --watch server.js",
    "check": "node --check server.js && node --check public/app.js"
  },
  "engines": {
    "node": ">=20"
  }
}
