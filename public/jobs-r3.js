(()=>{
'use strict';
const VERSION='5.3.0-r3';
const CONFIG=window.CYBERTRMX_BACKEND||{};
const SEC=window.CYBERTRMX_SECURITY||{};
const ACTIVE_STATES=new Set(['queued','validating','running','retry_wait']);
const RETRY_STATES=new Set(['failed','timed_out','cancelled','dead_letter']);
const ENABLED=location.pathname.includes('/staging/')||new URLSearchParams(location.search).get('jobs_r3')==='1';
const $=(selector,root=document)=>root.querySelector(selector);
const $$=(selector,root=document)=>[...root.querySelectorAll(selector)];
const safe=(value,max=300)=>String(value??'').replace(/[<>]/g,'').trim().slice(0,max);
const statusLabel=value=>String(value||'unknown').replaceAll('_',' ').toUpperCase();
const notify=message=>window.toast?window.toast(message):console.log(message);
let queueTimer=null,mounted=false,latestQueue=null,terminalBusy=false;
const jobTimers=new Map();
const requestState={lastEndpoint:'',lastAction:'',lastStatus:null,lastRequestId:'',lastBackendVersion:'',lastError:'',lastAt:'',lastDurationMs:0};
class JobApiError extends Error{constructor(payload,status=0){super(payload?.message||payload?.error||'The persistent job request failed.');this.code=payload?.error||'JOB_REQUEST_FAILED';this.status=status;this.requestId=payload?.request_id||'';this.retryable=Boolean(payload?.retryable)}}
function enabled(){return ENABLED&&Boolean(CONFIG.jobsFunction&&CONFIG.url&&CONFIG.publishableKey)}
function client(){return window.CYBERTRMX_SECURE_CLIENT||null}
async function activeSession(){const current=client();if(!current?.auth?.getSession)throw new JobApiError({error:'JOB_CLIENT_NOT_READY',message:'The protected account client is not ready.'});const result=await current.auth.getSession();const active=result?.data?.session;if(!active?.access_token)throw new JobApiError({error:'AUTH_REQUIRED',message:'Sign in before using the persistent job queue.'},401);return active}
function deviceContext(){let id='';try{id=SEC.getDeviceId?.()||localStorage.getItem('cybertrmx-device-id-v1')||''}catch{}if(!/^[0-9a-f-]{36}$/i.test(id)){id=crypto.randomUUID();try{localStorage.setItem('cybertrmx-device-id-v1',id)}catch{}}const meta=SEC.deviceMeta?.()||{label:'Device / Browser',platform:'Device',browser:'Browser'};return{id,meta}}
function recordRequest(action,response,payload,error,started){requestState.lastEndpoint=CONFIG.jobsFunction||'cybertrmx-jobs';requestState.lastAction=action;requestState.lastAt=new Date().toISOString();requestState.lastDurationMs=Math.max(0,Math.round(performance.now()-started));requestState.lastError=error?String(error.message||error):'';requestState.lastStatus=response?.status??null;requestState.lastRequestId=payload?.request_id||response?.headers?.get?.('x-request-id')||'';requestState.lastBackendVersion=payload?.backend_version||response?.headers?.get?.('x-cybertrmx-backend')||'';window.dispatchEvent(new CustomEvent('cybertrmx:diagnostics',{detail:{...requestState,source:'jobs-r3'}}))}
async function call(action,payload={}){if(!enabled())throw new JobApiError({error:'JOB_QUEUE_DISABLED',message:'Persistent jobs are enabled only in staging.'},409);const session=await activeSession();const{id,meta}=deviceContext();const headers={'Content-Type':'application/json',apikey:CONFIG.publishableKey,Authorization:`Bearer ${session.access_token}`,'x-device-id':id,'x-device-label':safe(meta.label,80).replace(/[^\x20-\x7E]/g,' '),'x-device-platform':safe(meta.platform,80).replace(/[^\x20-\x7E]/g,' '),'x-device-browser':safe(meta.browser,80).replace(/[^\x20-\x7E]/g,' '),'x-client-version':VERSION,'x-idempotency-key':crypto.randomUUID()};const started=performance.now();let response=null,body=null,lastError=null;for(let attempt=0;attempt<2;attempt++){const controller=new AbortController(),timeout=setTimeout(()=>controller.abort(),30000);try{response=await fetch(`${CONFIG.url}/functions/v1/${CONFIG.jobsFunction}`,{method:'POST',headers,body:JSON.stringify({action,...payload}),cache:'no-store',credentials:'omit',signal:controller.signal});body=await response.json().catch(()=>({error:`JOB_HTTP_${response.status}`,message:'The job service returned an unreadable response.'}));clearTimeout(timeout);if(!response.ok||body?.error)throw new JobApiError(body,response.status);recordRequest(action,response,body,null,started);return body}catch(error){clearTimeout(timeout);lastError=error instanceof JobApiError?error:new JobApiError({error:error?.name==='AbortError'?'JOB_TIMEOUT':'JOB_NETWORK_ERROR',message:error?.name==='AbortError'?'The job service did not respond before the timeout.':'The job service could not be reached.',retryable:true},0);if(lastError.status>0)break;if(attempt===0)await new Promise(resolve=>setTimeout(resolve,350))}}recordRequest(action,response,body,lastError,started);throw lastError}
function age(value){if(!value)return'—';const seconds=Math.max(0,Math.round((Date.now()-new Date(value).getTime())/1000));if(seconds<60)return`${seconds}s ago`;if(seconds<3600)return`${Math.floor(seconds/60)}m ago`;if(seconds<86400)return`${Math.floor(seconds/3600)}h ago`;return`${Math.floor(seconds/86400)}d ago`}
function jobInput(job){return safe(job?.request_payload?.input||'Input unavailable',253)}
function canCancel(status){return ACTIVE_STATES.has(status)}
function canRetry(status){return RETRY_STATES.has(status)}
function mount(){if(mounted&&$('#ops-r3-queue-card'))return true;const lookup=$('#ops-lookup-form'),reference=lookup?.closest('.ops-card');if(!lookup||!reference)return false;const card=document.createElement('article');card.id='ops-r3-queue-card';card.className='ops-card wide ops-private-card';card.hidden=reference.hidden;card.innerHTML=`<div class="ops-card-head"><div><small>PERSISTENT QUEUE / STAGING R3</small><h3>Job queue</h3></div><div class="r2-queue-head-actions"><span class="ops-live">LEASE + HEARTBEAT</span><button class="ops-mini" id="r3-refresh-queue">REFRESH</button></div></div><div class="r2-queue-summary"><div><small>QUEUED</small><strong id="r3-count-queued">0</strong><span>waiting for claim</span></div><div><small>ACTIVE</small><strong id="r3-count-active">0</strong><span>worker heartbeat</span></div><div><small>RETRY</small><strong id="r3-count-retry">0</strong><span>backoff window</span></div><div><small>DEAD LETTER</small><strong id="r3-count-dead">0</strong><span>manual review</span></div></div><div class="r2-queue-list" id="r3-queue-list"><div class="ops-empty">Queue status will appear after sign-in.</div></div><div class="r2-worker-note"><b>PERSISTENT QUEUE RECEIPT</b> · A successful submission must show a queue job ID. If this card is unavailable, the staging form will not fall back to the legacy synchronous job path.</div>`;reference.insertAdjacentElement('afterend',card);$('#r3-refresh-queue').onclick=()=>refreshQueue(false);new MutationObserver(()=>{card.hidden=reference.hidden}).observe(reference,{attributes:true,attributeFilter:['hidden']});mounted=true;return true}
function setCount(id,value){const element=$(`#${id}`);if(element)element.textContent=String(value)}
function renderQueue(data){if(!mount())return;latestQueue=data;const counts=new Map((data?.counts||[]).map(item=>[item.status,Number(item.count)||0]));setCount('r3-count-queued',counts.get('queued')||0);setCount('r3-count-active',(counts.get('running')||0)+(counts.get('validating')||0));setCount('r3-count-retry',counts.get('retry_wait')||0);setCount('r3-count-dead',counts.get('dead_letter')||0);const jobs=data?.jobs||[],list=$('#r3-queue-list');if(!list)return;list.innerHTML=jobs.length?jobs.map(job=>`<div class="r2-queue-row" data-state="${safe(job.status,30)}"><div class="r2-queue-main"><div class="r2-queue-badge ${safe(job.status,30)}">${statusLabel(job.status)}</div><strong>${statusLabel(job.job_type)}</strong><em>${jobInput(job)}</em><small>${Number(job.progress)||0}% / ATTEMPT ${Number(job.attempt_count)||0}/${Number(job.max_attempts)||0} / ${age(job.heartbeat_at||job.claimed_at||job.created_at)}</small></div><div class="r2-queue-actions"><button class="ops-mini" data-r3-open="${job.id}">EVENTS</button>${canCancel(job.status)?`<button class="ops-mini danger" data-r3-cancel="${job.id}">CANCEL</button>`:''}${canRetry(job.status)?`<button class="ops-mini" data-r3-retry="${job.id}">RETRY</button>`:''}</div></div>`).join(''):'<div class="ops-empty">No persistent jobs have been queued.</div>';$$('[data-r3-open]',list).forEach(button=>button.onclick=()=>openJob(button.dataset.r3Open));$$('[data-r3-cancel]',list).forEach(button=>button.onclick=()=>cancelJob(button.dataset.r3Cancel));$$('[data-r3-retry]',list).forEach(button=>button.onclick=()=>retryJob(button.dataset.r3Retry))}
function renderQueueError(error){if(!mount())return;const list=$('#r3-queue-list');if(list)list.innerHTML=`<div class="r2-queue-error"><strong>QUEUE UNAVAILABLE</strong><span>${safe(error.message,300)}</span>${error.requestId?`<small>REQUEST ${safe(error.requestId,80)}</small>`:''}</div>`}
async function refreshQueue(silent=true){if(!enabled()||document.visibilityState==='hidden'||!$('#view-operations')?.classList.contains('active'))return null;try{const data=await call('queue_status');renderQueue(data);return data}catch(error){renderQueueError(error);if(!silent)notify(error.message);return null}}
function scheduleQueue(){clearTimeout(queueTimer);queueTimer=setTimeout(async()=>{await refreshQueue(true);scheduleQueue()},5000)}
function summarize(result){let value=result;for(let i=0;i<3&&typeof value==='string';i++){try{value=JSON.parse(value)}catch{break}}if(!value)return'No result returned.';if(value.records)return Object.entries(value.records).map(([type,records])=>`${type}: ${Array.isArray(records)?records.length:0} record(s)`).join('\n');if(value.response){const data=value.response;return JSON.stringify({query:value.query,source:value.source,ip:data.ip||null,name:data.name||data.handle||data.ldhName||null,country:data.country||data.country_code||null,region:data.region||null,city:data.city||null,asn:data.connection?.asn||data.startAutnum||null,org:data.connection?.org||data.name||null,timezone:data.timezone?.id||null},null,2)}return JSON.stringify(value,null,2).slice(0,7000)}
function renderJob(job,events=[]){const consoleBox=$('#ops-job-console'),result=$('#ops-job-result');if(!consoleBox)return;consoleBox.hidden=false;$('#ops-job-label').textContent=statusLabel(job.status);$('#ops-job-progress').textContent=`${Number(job.progress)||0}%`;$('#ops-job-bar').style.width=`${Math.max(0,Math.min(100,Number(job.progress)||0))}%`;const eventBox=$('#ops-job-events');if(eventBox)eventBox.innerHTML=events.map(event=>`<div class="ops-job-event"><b>${String(Number(event.progress)||0).padStart(2,'0')}%</b><span>${statusLabel(event.stage)}</span><em>${safe(event.message,260)}</em></div>`).join('');const failed=['failed','timed_out','dead_letter','cancelled'].includes(job.status);if(result){result.hidden=!(job.result||failed);result.textContent=job.result?summarize(job.result):failed?`${statusLabel(job.status)}${job.last_error_code?` / ${job.last_error_code}`:''}${job.last_error_message?`\n${job.last_error_message}`:''}`:''}let controls=$('#r3-job-controls');if(!controls){controls=document.createElement('div');controls.id='r3-job-controls';controls.className='r2-job-controls';consoleBox.insertAdjacentElement('afterend',controls)}controls.innerHTML=`${canCancel(job.status)?`<button class="ops-button secondary" data-r3-console-cancel="${job.id}">CANCEL JOB</button>`:''}${canRetry(job.status)?`<button class="ops-button" data-r3-console-retry="${job.id}">RETRY JOB</button>`:''}`;$('[data-r3-console-cancel]',controls)?.addEventListener('click',()=>cancelJob(job.id));$('[data-r3-console-retry]',controls)?.addEventListener('click',()=>retryJob(job.id))}
async function pollJob(jobId,scroll=false){clearTimeout(jobTimers.get(jobId));const data=await call('job_status',{job_id:jobId});renderJob(data.job,data.events||[]);if(scroll)$('#ops-job-console')?.scrollIntoView({behavior:'smooth',block:'center'});await refreshQueue(true);window.CYBERTRMX_OPERATIONS?.refresh?.(true);if(ACTIVE_STATES.has(data.job.status)){const timer=setTimeout(()=>pollJob(jobId,false).catch(()=>{}),Math.max(1000,Number(data.poll_after_ms)||2000));jobTimers.set(jobId,timer)}else jobTimers.delete(jobId);return data}
async function openJob(jobId){try{return await pollJob(jobId,true)}catch(error){notify(error.message)}}
async function enqueue(jobType,input,scroll=true){const data=await call('run_lookup',{job_type:jobType,input,case_id:localStorage.getItem('cybertrmx-active-case')||null});notify(`Persistent queue receipt / ${String(data.job_id).slice(0,8)}`);terminalLine(`PERSISTENT QUEUE RECEIPT ${data.job_id}`,'ok');await pollJob(data.job_id,scroll);return data}
async function enqueueFromForm(){const type=$('#ops-lookup-type')?.value,input=safe($('#ops-lookup-input')?.value,253),button=$('#ops-run-lookup');if(!input){notify('Enter a domain or public IPv4 address');return}if(button){button.disabled=true;button.textContent='QUEUEING…'}const consoleBox=$('#ops-job-console');if(consoleBox)consoleBox.hidden=false;if($('#ops-job-label'))$('#ops-job-label').textContent='PERSISTENT QUEUE';if($('#ops-job-progress'))$('#ops-job-progress').textContent='0%';if($('#ops-job-bar'))$('#ops-job-bar').style.width='0%';if($('#ops-job-events'))$('#ops-job-events').innerHTML='<div class="ops-job-event"><b>00%</b><span>PERSISTENT QUEUE</span><em>Writing the job to PostgreSQL. The legacy operation path is disabled in staging.</em></div>';try{await enqueue(type,input,false)}catch(error){notify(error.message);const result=$('#ops-job-result');if(result){result.hidden=false;result.textContent=`QUEUE ERROR\n${error.message}${error.requestId?`\nRequest ${error.requestId}`:''}`}}finally{if(button){button.disabled=false;button.textContent='RUN JOB'}}}
async function cancelJob(jobId){if(!confirm('Cancel this queued or running job?'))return;try{const data=await call('cancel_job',{job_id:jobId});notify(data.job?.status==='cancelled'?'Job cancelled':'Cancellation requested');await pollJob(jobId,false)}catch(error){notify(error.message)}}
async function retryJob(jobId){try{const data=await call('retry_job',{job_id:jobId});notify(`Retry queued / ${String(data.job_id).slice(0,8)}`);await pollJob(data.job_id,true)}catch(error){notify(error.message)}}
function terminalLine(text,type=''){const output=$('#terminal-output');if(!output)return;const row=document.createElement('div');row.className=`line ${type}`;row.textContent=text;output.append(row);output.scrollTop=output.scrollHeight}
async function terminalJob(raw){const parts=raw.trim().split(/\s+/),command=(parts[0]||'').toLowerCase();if(command==='lookup'){const map={dns:'dns_inventory',rdap:'rdap_lookup',ip:'ip_enrichment'},jobType=map[(parts[1]||'').toLowerCase()],input=safe(parts[2],253);if(!jobType||!input){terminalLine('usage: lookup <dns|rdap|ip> <domain-or-ip>','warn');return}await enqueue(jobType,input,false);return}const sub=(parts[1]||'list').toLowerCase(),jobId=parts[2]||'';if(sub==='list'||sub==='queue'){const data=await call('queue_status');if(!(data.jobs||[]).length){terminalLine('persistent queue is empty','block');return}(data.jobs||[]).slice(0,12).forEach(job=>terminalLine(`${job.id}  ${statusLabel(job.status)}  ${Number(job.progress)||0}%  ${jobInput(job)}`,'block'));return}if(!jobId){terminalLine('usage: job <status|cancel|retry> <job-id>','warn');return}if(sub==='status'){const data=await call('job_status',{job_id:jobId});terminalLine(`${data.job.id}  ${statusLabel(data.job.status)}  ${data.job.progress}%  attempt ${data.job.attempt_count}/${data.job.max_attempts}`,'block');return}if(sub==='cancel'){const data=await call('cancel_job',{job_id:jobId});terminalLine(`job ${jobId} / ${statusLabel(data.job.status)}`,'ok');return}if(sub==='retry'){const data=await call('retry_job',{job_id:jobId});terminalLine(`retry queued: ${data.job_id}`,'ok');return}terminalLine('usage: job <list|status|cancel|retry> [job-id]','warn')}
function isJobCommand(raw){return /^(?:job|lookup)(?:\s|$)/i.test(String(raw||'').trim())}
async function interceptTerminal(raw){if(terminalBusy||!isJobCommand(raw))return false;terminalBusy=true;const input=$('#command-input');if(input)input.value='';terminalLine(`root@trmx# ${raw}`,'command');try{await terminalJob(raw)}catch(error){terminalLine(`job operation failed: ${error.message}`,'warn')}finally{terminalBusy=false}return true}
function installEarlyGuards(){
  document.addEventListener('submit',event=>{if(event.target?.id==='ops-lookup-form'){event.preventDefault();event.stopImmediatePropagation();enqueueFromForm();return}if(event.target?.id==='terminal-form'){const raw=$('#command-input')?.value.trim()||'';if(!isJobCommand(raw))return;event.preventDefault();event.stopImmediatePropagation();interceptTerminal(raw)}},true);
  document.addEventListener('keydown',event=>{if(event.key!=='Enter'||event.target?.id!=='command-input')return;const raw=event.target.value.trim();if(!isJobCommand(raw))return;event.preventDefault();event.stopImmediatePropagation();interceptTerminal(raw)},true);
  document.addEventListener('click',event=>{const button=event.target.closest?.('#terminal-form button,#ops-run-lookup');if(!button)return;if(button.id==='ops-run-lookup'){event.preventDefault();event.stopImmediatePropagation();enqueueFromForm();return}const raw=$('#command-input')?.value.trim()||'';if(!isJobCommand(raw))return;event.preventDefault();event.stopImmediatePropagation();interceptTerminal(raw)},true);
}
function extendHints(){const api=window.CYBERTRMX_COMMAND_HINTS;if(!api?.commands)return false;const items=[{command:'job list',category:'Jobs',description:'List persistent jobs in the connected workspace.'},{command:'job status <job-id>',category:'Jobs',description:'Show status, progress, and attempt count.'},{command:'job cancel <job-id>',category:'Jobs',description:'Cancel a queued or running persistent job.'},{command:'job retry <job-id>',category:'Jobs',description:'Queue a new attempt from a terminal job state.'}];items.forEach(item=>{if(!api.commands.some(existing=>existing.command===item.command))api.commands.push(item)});return true}
function initialize(){if(!enabled())return;installEarlyGuards();let attempts=0;const wait=setInterval(()=>{attempts++;mount();if(client()&&mount()){clearInterval(wait);refreshQueue(true);scheduleQueue()}else if(attempts>=120){clearInterval(wait);mount();renderQueueError(new JobApiError({message:'The staging job client did not become ready.'}))}},100);if(!extendHints()){let tries=0;const hints=setInterval(()=>{tries++;if(extendHints()||tries>80)clearInterval(hints)},100)}document.addEventListener('visibilitychange',()=>{if(document.visibilityState==='visible')refreshQueue(true)});document.addEventListener('click',event=>{if(event.target.closest?.('[data-go="operations"],[data-tab="operations"]'))setTimeout(()=>refreshQueue(true),350)},true)}
window.CYBERTRMX_JOBS_R3={version:VERSION,enabled:enabled(),call,refresh:refreshQueue,open:openJob,cancel:cancelJob,retry:retryJob,diagnostics:()=>({...requestState}),latest:()=>latestQueue};
if(document.readyState==='loading')document.addEventListener('DOMContentLoaded',initialize,{once:true});else initialize();
})();