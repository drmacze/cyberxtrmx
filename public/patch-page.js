(()=>{
'use strict';
const releases=[
 {version:'5.3.0-r2',date:'05 AUG 2026',title:'Safe persistent jobs — staging candidate',items:['Connected the existing PostgreSQL queue and worker through an isolated staging-only module.','Lookup submissions now return a queue receipt immediately, then continue through claim, lease, heartbeat, retry, cancellation, and dead-letter states after the browser closes.','Added queue counts, event history, cancel, retry, and terminal job commands without modifying Home, core navigation, or the verified Operations runtime.','The module uses direct authenticated requests and does not replace the Supabase client, window.fetch, or Operations API. A queue failure remains inside the queue panel and cannot turn the main workspace into Sync Error.']},
 {version:'5.2.8',date:'05 AUG 2026',title:'Production Guard',items:['Created an isolated staging branch and preview path so new frontend work no longer starts in production.','Added System Diagnostics in Profile and terminal commands for build, cache, service worker, device identity, session, request ID, backend version, and page errors.','Added a controlled clean reload that replaces application caches without deleting account sessions or workspace data.','Replaced stale quality rules with contracts for the active recovery path, locked frontend baseline, Patch route, Operations diagnostics, and production/staging deployment.']},
 {version:'5.2.7',date:'05 AUG 2026',title:'Deterministic Operations and Patch recovery',items:['Removed the separate Locations request from the active browser path and now reuse location data included in the dashboard response.','Removed the previous Patch and transport loaders from the connected bootstrap path.','Added one versioned recovery module that loads before Supabase and provides a built-in Patch fallback when the release module cannot load.']},
 {version:'5.2.6',date:'05 AUG 2026',title:'Operations and Patch recovery',items:['Removed the redundant Locations network request from the Operations refresh path and now reuse location data already included in the dashboard response.','Rebuilt the Patch loader so stale or failed script tags are removed and the release module is loaded again with a fresh URL.','Added both pointer and click handling for reliable Patch taps while keeping the locked interface unchanged.']},
 {version:'5.2.5',date:'05 AUG 2026',title:'Patch navigation recovery',items:['Replaced the one-shot Patch wait with a loader that waits until the release module is actually ready.','Added a capture-level Patch click handler so taps remain reliable while connected modules are loading.','Kept the locked layout, animation, and core navigation files unchanged.']},
 {version:'5.2.4',date:'05 AUG 2026',title:'Locations CORS recovery',items:['Expanded the Locations endpoint preflight allowlist for authenticated clients.','Confirmed the Operations API was healthy while the Locations request stopped after OPTIONS.','Kept the locked interface and frontend core unchanged.']},
 {version:'5.2.3',date:'05 AUG 2026',title:'Operations transport recovery',items:['Combined identical dashboard reads to avoid overlapping requests.','Added a Safari-compatible timeout and one controlled retry.','Normalized custom request headers while keeping the locked interface unchanged.']},
 {version:'5.2.2',date:'05 AUG 2026',title:'Frontend stability lock',items:['Locked the verified frontend baseline.','Added Chromium and iPhone WebKit smoke tests.','Added backend-failure and asset-integrity checks.']},
 {version:'5.2.1',date:'05 AUG 2026',title:'Stable frontend recovery',items:['Restored the complete frontend tree to the last verified 5.2 snapshot.','Removed withdrawn 5.3 browser modules from production.','Reset the PWA cache to restore the stable interface.']},
 {version:'5.2.0',date:'05 AUG 2026',title:'Security and reliability foundation',items:['Added request IDs, device identity, rate limits, and idempotency.','Added authenticator MFA, device controls, and security events.','Hardened check-in submissions and restored provider-backed collection.']},
 {version:'5.1.0',date:'05 AUG 2026',title:'Terminal command clues',items:['Added contextual command suggestions, categories, and mobile tap support.']},
 {version:'5.0.0',date:'04 AUG 2026',title:'Connected operations',items:['Added account-backed workspaces, cases, jobs, evidence, audit history, and secure check-in.']}
];
function goHome(){const button=document.querySelector('.nav-item[data-tab="overview"]');if(button)button.click();else document.querySelectorAll('.view').forEach(view=>view.classList.toggle('active',view.id==='view-overview'))}
function openOperations(){if(window.CYBERTRMX_OPERATIONS)window.CYBERTRMX_OPERATIONS.open();else window.toast?.('Operations is still connecting')}
function openDiagnostics(){if(window.CYBERTRMX_GUARD)window.CYBERTRMX_GUARD.open();else window.toast?.('System Guard is still loading')}
function build(){const main=document.querySelector('main');if(!main)return;document.querySelector('#view-patch')?.remove();const view=document.createElement('section');view.className='view';view.id='view-patch';view.innerHTML=`<div class="patch-page"><section class="patch-hero"><div><button class="patch-back" id="patch-back">← BACK TO WORKSPACE</button><small>CYBERTRMX / STAGING RELEASE NOTES</small><h2>PATCH<br>5.3.0-r2</h2><p>Persistent jobs return through an isolated staging module without changing the verified production interface.</p><div class="patch-version"><i></i> CURRENT BUILD / 5.3.0-r2</div></div></section><section class="patch-checkin"><div><small>STAGING CANDIDATE</small><h3>Queue work, close the browser, and return to the same job state.</h3><p>Operations now exposes queue counts, worker events, heartbeat state, cancellation, retries, and dead-letter review. Production remains on 5.2.8 until this candidate is verified on iPhone.</p></div><div class="patch-checkin-actions"><button id="open-operations">OPEN OPERATIONS</button><button id="open-diagnostics">OPEN DIAGNOSTICS</button></div></section><section class="patch-list">${releases.map(release=>`<article class="patch-entry"><time>${release.date}<br>v${release.version}</time><div><h3>${release.title}</h3><ul>${release.items.map(item=>`<li>${item}</li>`).join('')}</ul></div></article>`).join('')}</section></div>`;main.append(view);view.querySelector('#patch-back').onclick=goHome;view.querySelector('#open-operations').onclick=openOperations;view.querySelector('#open-diagnostics').onclick=openDiagnostics}
function open(){if(!document.querySelector('#view-patch'))build();document.querySelectorAll('.view').forEach(view=>view.classList.toggle('active',view.id==='view-patch'));const title=document.querySelector('#title'),crumb=document.querySelector('#breadcrumb');if(title)title.textContent='Patch Notes';if(crumb)crumb.textContent='WORKSPACE / PATCH';window.scrollTo({top:0,behavior:'smooth'})}
function init(){build();window.CYBERTRMX_PATCH={open,rebuild:build}}
if(document.readyState==='loading')document.addEventListener('DOMContentLoaded',init,{once:true});else init();
})();