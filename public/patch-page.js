(()=>{
const releases=[
 {version:'4.6.0',date:'04 AUG 2026',title:'Secure check-in',items:['Added consent-based location check-in link.','Added event-driven location acquisition.','Added Maps and share actions.']},
 {version:'4.5.0',date:'04 AUG 2026',title:'Tracker engine',items:['Real permission and sensor stages.','Multi-sample accuracy and stability checks.','Movement and timeout reporting.']},
 {version:'4.4.0',date:'03 AUG 2026',title:'Intel workspace',items:['Separated Intel tools.','Fixed iPhone overflow.','Moved alerts to the top.']}
];
function goHome(){const b=document.querySelector('.nav-item[data-tab="overview"]');if(b)b.click();else document.querySelectorAll('.view').forEach(v=>v.classList.toggle('active',v.id==='view-overview'))}
function copyCheckin(){const url=new URL('./checkin.html',location.href).href;navigator.clipboard?.writeText(url).then(()=>window.toast?.('Check-in link copied')).catch(()=>window.prompt('Copy this link',url))}
function build(){const main=document.querySelector('main');if(!main)return;document.querySelector('#view-patch')?.remove();const view=document.createElement('section');view.className='view';view.id='view-patch';view.innerHTML=`<div class="patch-page"><section class="patch-hero"><div><button class="patch-back" id="patch-back">← BACK TO WORKSPACE</button><small>CYBERTRMX / RELEASE NOTES</small><h2>PATCH<br>4.6</h2><p>Latest system changes, condensed to the essentials.</p><div class="patch-version"><i></i> CURRENT BUILD / 4.6.0</div></div></section><section class="patch-checkin"><div><small>SECURE LOCATION CHECK-IN</small><h3>Share a consent-based location request.</h3><p>The recipient sees what will be shared and must approve browser location access before any coordinate is produced.</p></div><div class="patch-checkin-actions"><a href="./checkin.html" target="_blank" rel="noopener">OPEN CHECK-IN</a><button id="copy-checkin">COPY LINK</button></div></section><section class="patch-list">${releases.map(r=>`<article class="patch-entry"><time>${r.date}<br>v${r.version}</time><div><h3>${r.title}</h3><ul>${r.items.map(i=>`<li>${i}</li>`).join('')}</ul></div></article>`).join('')}</section></div>`;main.append(view);view.querySelector('#patch-back').onclick=goHome;view.querySelector('#copy-checkin').onclick=copyCheckin;}
function open(){if(!document.querySelector('#view-patch'))build();document.querySelectorAll('.view').forEach(v=>v.classList.toggle('active',v.id==='view-patch'));const title=document.querySelector('#title'),crumb=document.querySelector('#breadcrumb');if(title)title.textContent='Patch Notes';if(crumb)crumb.textContent='WORKSPACE / PATCH';window.scrollTo({top:0,behavior:'smooth'})}
function init(){build();window.CYBERTRMX_PATCH={open};}
if(document.readyState==='loading')document.addEventListener('DOMContentLoaded',init,{once:true});else init();
})();